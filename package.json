{
  "name": "botwp-monorepo",
  "version": "1.0.0",
  "description": "Proyecto unificado BOTWP (Bitácora + Server)",
  "scripts": {
    "start": "node index.js",
    "install-all": "npm install && cd server && npm install && cd ../bitacora && npm install",
    "build": "cd bitacora && npm run build",
    "dev": "concurrently \"cd server && npm run dev\" \"cd bitacora && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
